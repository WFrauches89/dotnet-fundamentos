using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Globalization;

namespace ExplorandoLinguagemCSharp.Arquivos
{
    public class ManipulandoS
    {
        /*
        CultureInfo.DefaultThreadCurrentCulture = new CultureInfo("en-us");
        CultureInfo.DefaultThreadCurrentCulture = new System.Globalization.CultureInfo("pt-BR");

        decimal valorMonetario = 15829.35M;
        double porcetagem = .81235;
        int cpf = 129538947;
        string cpf2 = "12953894799";

        Console.WriteLine($"{valorMonetario:C}");
        Console.WriteLine($"{valorMonetario:C8}");
        Console.WriteLine($"{valorMonetario:N5}");

        Console.WriteLine(porcetagem.ToString("P"));
        Console.WriteLine(porcetagem.ToString("F"));
        Console.WriteLine(porcetagem.ToString("P5"));

        Console.WriteLine(cpf.ToString("###-###-###"));
        Console.WriteLine($"{cpf2.Substring(0, 3)}.{cpf2.Substring(3, 3)}.{cpf2.Substring(6, 3)}-{cpf2.Substring(9, 2)}");

        DateTime date = DateTime.Now;
        Console.WriteLine(date);
        Console.WriteLine(date.ToShortDateString());
        Console.WriteLine(date.ToShortTimeString());
        Console.WriteLine(date.ToString("d-M-yy HH:mm"));
        Console.WriteLine(date.ToString("d-M-yy hh:mm") + " - Horas em 12");
        Console.WriteLine(date.ToString("dd/MM/yyyy HH:mm"));

        DateTime dateConvert = DateTime.Parse("03-03-89 06:52");
        //Não lida com datas erradas - crashing
        Console.WriteLine(dateConvert);

        string dateStringTo = "2009-14-20 17:21";
        //Lida com datas erradas - com exceptions data inválida
        bool tryParseDate = DateTime.TryParseExact(dateStringTo,
                                "yyyy-MM-dd HH:mm",
                                CultureInfo.InvariantCulture,
                                DateTimeStyles.None,
                                out DateTime dateConvert2);
        if public ManipulandoS()
        {
        }

        (tryParseDate)
        {
            Console.WriteLine($"Conversão realizzada com sucesso! {dateConvert2}");
        }
        else
        {
            Console.WriteLine($"{dateStringTo} não é uma data válida!");
        }
        */
    }
}